---
- name: Copy file
  hosts: aws
  tasks:
    - name: Copy a file
      copy:
        src: ./file.txt
        dest: /tmp/file.txt
      register: output
    - name: debug
      debug:
        msg: "File has been copied successfully"
      when: output.changed

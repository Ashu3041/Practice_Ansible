---
- name: Create user
  hosts: aws
  become: yes
  vars_files: 
    - vars_file.yaml

  tasks:
    - name: Creating user
      user:
        name: "{{ username }}"
        shell: /bin/bash
        groups: sudo
        append: yes
